[package]
name = "inceptool"
version = "0.1.0"
edition = "2024"
authors = ["Mikalai Seva"]
description = "An extensible LLM agent hook architecture"
license = "MIT"
repository = "https://github.com/asakura/inceptool"
readme = "README.md"
keywords = ["llm", "agents", "hooks", "ai"]
categories = ["development-tools"]

[workspace]
members = [
    "crates/protocol",
    "crates/driver-claude",
    "crates/driver-gemini",
]

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", features = ["raw_value"] }
thiserror = "2.0"
rstest = "0.26.1"
strum = "0.28.0"
strum_macros = "0.28.0"
inceptool_protocol = { package = "inceptool-protocol", path = "crates/protocol" }
driver-claude = { package = "inceptool-driver-claude", path = "crates/driver-claude" }
driver-gemini = { package = "inceptool-driver-gemini", path = "crates/driver-gemini" }

[dependencies]
inceptool_protocol.workspace = true
driver-claude.workspace = true
driver-gemini.workspace = true
